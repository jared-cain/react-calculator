
var getTotal = function(equation) {
    console.log(`getTotal running: ${equation}`);

    var workingEquation = equation;
    // order of operations: multiply, divide, add subtract
    if (workingEquation.indexOf("*") != -1){
        var multInd = workingEquation.indexOf("*");
        for (var i = 0; i < array.length; i++) {
            array[i]
        }
    }

};


module.exports = getTotal;
